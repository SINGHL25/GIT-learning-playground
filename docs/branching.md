

### `docs/branching.md`
```markdown
# Branching

- create: `git branch feature`
- switch: `git checkout feature` or `git switch feature`
- create+switch: `git checkout -b feature`
- merge into main:
  - `git checkout main`
  - `git merge feature`
- rebase: `git rebase main` (advanced)
